\set ON_ERROR_ROLLBACK interactive

-- change the null display character
\pset null 'Ø'

-- automatically switch between extended and normal
\x auto

-- always show how long a query takes
\timing

-- wrap long lines selecting text columns
\pset format wrapped
